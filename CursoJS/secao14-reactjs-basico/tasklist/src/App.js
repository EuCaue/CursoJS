import React from 'react';
import Main from './components/Main';
import './App.css';

// Só pode retornar um elemento de HTML, restantes dos elementos devem estar dentro dele
// Podendo ser uma <div> </div> ou caso não queria colocar nenhum elemento
// <> </> caso não queira usar nenhum elemento

export default function App() {
  return <Main />;
}
